import { render } from "@testing-library/react";
import { ResponseTab } from "@/helpers";

describe("ResponseTab", () => {
  test("Deve renderizar o ResponseTab", () => {
    render(<ResponseTab />);
  });
});
