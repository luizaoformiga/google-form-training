import { render } from "@testing-library/react";
import { DragAndDrop } from "@/components";

describe("DragAndDrop", () => {
  test("Deve renderizar o DragAndDrop", () => {
    render(<DragAndDrop />);
  });
});
