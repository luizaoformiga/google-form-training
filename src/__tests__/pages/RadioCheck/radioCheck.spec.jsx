import { render } from "@testing-library/react";
import { RadioCheck } from "@/pages";

describe("RadioCheck", () => {
  test("Deve renderizar o RadioCheck", () => {
    render(<RadioCheck />);
  });
});
